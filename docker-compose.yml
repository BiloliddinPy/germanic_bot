version: "3.9"

services:
  bot:
    build: .
    container_name: germanic_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./backups:/app/backups
    command: ["python", "main.py"]
